## Math Expressions
### Inline, by itself
$e^x$
### Inline with text, snipping only the expression
The exponential function $e^x$ is its own derivative.
### Inline with text, snipping the full line
The exponential function $e^x$ is its own derivative.
### Multi-line, by itself
$$e^x$$
### Multi-line with text, snipping only the expression
The exponential function $$e^x$$ is its own derivative.
### Multi-line with text, snipping the full line
The exponential function $$e^x$$ is its own derivative.